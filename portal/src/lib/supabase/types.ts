/**
 * Re-export everything from the shared package.
 * This file exists so that existing imports like `@/lib/supabase/types`
 * and `./types` continue to work without changing every file.
 */
export * from "@mecanova/shared";
